import React from 'react';
import { useParams } from 'react-router-dom';

const UpdateBook = () => {
    const {id} = useParams;
    return (
        <div>
            
        </div>
    );
};

export default UpdateBook;